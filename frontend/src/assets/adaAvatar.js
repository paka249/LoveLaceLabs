// Single source of truth for Ada's avatar image, used by the chat bubble and
// the sidebar entry. Swap the image by replacing ada_bot.png (same filename)
// or pointing this import at a different file - nothing else needs to change.
import adaBotImg from './ada_bot.png';

export default adaBotImg;
